# Maïoun — la page de présentation.
#
# CELLE-CI, AU CONTRAIRE DE L'APPLICATION, a vocation à être trouvée : c'est
# elle qu'un moteur doit montrer à qui cherche un service d'alertes de location
# à Nice. L'application, derrière la connexion, reste interdite par son propre
# `robots.txt` et par sa balise `noindex`.

User-agent: *
Allow: /

# ATTENTION, CE FICHIER EST INERTE TANT QUE LE SITE VIT DANS UN SOUS-CHEMIN.
# Un robot ne lit `robots.txt` qu'à la RACINE d'un domaine : sur
# `wizyxgh.github.io/RentFinder/`, la racine appartient à GitHub, pas à nous.
# Ce fichier ne prendra effet qu'avec un domaine propre. D'ici là, c'est la
# balise `robots` de la page qui parle — et c'est elle qu'il faut vérifier.

Sitemap: https://wizyxgh.github.io/RentFinder/sitemap.xml
